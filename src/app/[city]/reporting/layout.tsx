/**
 * Reporting Dashboard Layout
 *
 * Layout wrapper for the reporting section.
 */

import type { ReactNode } from 'react';

interface ReportingLayoutProps {
  children: ReactNode;
}

export default function ReportingLayout({ children }: ReportingLayoutProps) {
  return children;
}
